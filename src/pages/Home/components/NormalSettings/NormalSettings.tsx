import Settings from "../Settings/Settings";

export default function NormalSettings() {
  return (
    <section aria-label='normal setting' className='flex w-72 rounded-l-md flex-col justify-between'>
      <Settings />
    </section>
  );
}
